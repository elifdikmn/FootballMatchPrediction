name: Sync live scores
on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/sync-live.yml
      - live_sync.py
      - live_repository.py
      - provider_cache.py
      - models.py
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
concurrency:
  group: live-scores
  cancel-in-progress: false
permissions:
  contents: read
jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
    steps:
      - name: Check required configuration
        id: config
        shell: bash
        run: |
          if [[ -z "$DATABASE_URL" || -z "$API_FOOTBALL_KEY" ]]; then
            echo "::warning::Live sync skipped: DATABASE_URL and API_FOOTBALL_KEY repository secrets are required."
            echo "configured=false" >> "$GITHUB_OUTPUT"
          else
            echo "configured=true" >> "$GITHUB_OUTPUT"
          fi
      - uses: actions/checkout@v5
        if: steps.config.outputs.configured == 'true'
      - uses: actions/setup-python@v6
        if: steps.config.outputs.configured == 'true'
        with:
          python-version: '3.12'
          cache: pip
      - run: pip install sqlalchemy psycopg[binary] python-dotenv requests
        if: steps.config.outputs.configured == 'true'
      - name: Refresh shared scores only during fixture windows
        if: steps.config.outputs.configured == 'true'
        run: python live_sync.py
